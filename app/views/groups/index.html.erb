<div class="container">
  <div class="row">
    <div class="col">
      <div>
        <% unless @my_groups.present? %>
          <p>現在登録されているグループはありません</p>
        <% else %>
          <h4 class="my-4">グループ一覧</h4>
          <%= render "index", my_groups: @my_groups %>
        <% end %>
      </div>
      <div>
        <h4 class="my-4">新規作成</h4>
        <%= render "new", group: @group %>
      </div>
    </div>
  </div>
</div>